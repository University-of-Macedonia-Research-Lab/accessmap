import { AboutContent } from "@/components/about-content";

export const metadata = {
  title: "About · AccessMap",
  description:
    "How AccessMap works: data-driven floor plans, A* routing with accessibility profiles, and an assistant that calls those same primitives as tools.",
};

export default function AboutPage() {
  return <AboutContent />;
}
