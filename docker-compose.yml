services:
  nextjs-app:
    build: .
    restart: always
    ports:
      - "3003:3000"
    environment:
      - NODE_ENV=production
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    networks:
      - shared_network
networks:
  shared_network:
    external: true
