# AccessMap env template — copy to .env and fill in.
# Currently optional — nothing in src/ imports the Prisma client, so the
# app boots fine with no env at all. Keep this here for the Anthropic key
# when the AI feature gets wired up.

# ANTHROPIC_API_KEY=
